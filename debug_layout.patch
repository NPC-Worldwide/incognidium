--- a/crates/incognidium-layout/src/lib.rs
+++ b/crates/incognidium-layout/src/lib.rs
@@ -1513,6 +1513,10 @@ fn layout_block(
         compute_layout(child, styles, abs_width, container_h, image_sizes);
 
+        eprintln!("DEBUG: Absolute child {:?} positioned at x={}, y={}, container_w={}, container_h={}",
+                  child.node_id, child.x, child.y, container_w, container_h);
+
         // For auto width, shrink-to-fit the content
         if is_auto_width {
             // Calculate the intrinsic width from the laid out content
